- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-create-installation
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-partial-update-installation
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-preferences
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-partial-update-preference
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-installation-requests
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-all-vendor-installations
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-tenant-by-installation-id
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-tenant-by-id
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-tenant-by-owner
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-set-centralized-repo
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-validate-centralized-repo
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get_integrations
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get_installations
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-activate-installation
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-user-by-vendor
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-create-user
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-handle-user-signup
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-update-user
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-all-users
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-create-user-vendor
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-users-vendor-by-vendor
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-user-by-vendor-and-vendor-id
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-create-vendor-tenant
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-cloud-formation-stack-listener
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-generate-tenant-invitation-token
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-partial-update-onboarding-details
- arn:aws:lambda:${aws:region}:${aws:accountId}:function:${self:service}-${self:provider.stage}-get-onboarding-details
